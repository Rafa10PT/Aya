package com.example.aya

import android.os.Handler
import android.content.Intent
import android.content.Context

class ServiceWrapper(handler: Handler) {
    private var m_handler: Handler = handler

    fun try_start_service(intent: Intent, context: Context) {

    }
}